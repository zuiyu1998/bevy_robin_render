[package]
name = "robin_render"
version = "0.1.0"
edition = "2024"

[dependencies]

bevy = { workspace = true }
wgpu = { version = "29.0.1", default-features = false, features = [
    "wgsl",
    "dx12",
    "metal",
    "vulkan",
    "naga-ir",
    "fragile-send-sync-non-atomic-wasm",
] }
