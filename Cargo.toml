[package]
name = "bevy_robin_render"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { git = "https://github.com/bevyengine/bevy.git", branch = "main", optional = true, features = [
    "bevy_render",
] }
